// marble-catcher — round collection bowl (quadri-plot MarbleCatcher). Marbles drop
// in and collect; no exit. Flat bottom sits on the table.
use <lib.scad>

module mr_marble_catcher() { marble_catcher(); }

mr_marble_catcher();
