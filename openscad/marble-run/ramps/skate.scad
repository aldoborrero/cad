// skate — the long orange "Mega Skatepark" ramp: a valley that sags in the vertical
// plane, with a flat node tab at the top end. Dimensions are estimated from a photo;
// see the note by SKATE_R in lib.scad.
use <../lib.scad>

module mr_skate() { skate_ramp(); }

mr_skate();
