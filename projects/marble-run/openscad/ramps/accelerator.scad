// accelerator — the red scoop-wedge ramp. It sits on a rail and launches a marble
// that drops onto it horizontally along the level rail (converts the vertical drop to
// speed). Not in quadri-plot; modelled from the real Hape "little red ramp". One piece,
// closed base with a central hendidura that grips the rail.
use <../lib.scad>

module mr_accelerator() { accelerator(); }

mr_accelerator();
